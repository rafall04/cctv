# Welcome to the chrony configuration file. See chrony.conf(5) for more
# information about usuable directives.

# This will use (up to):
# - 4 sources from ntp.ubuntu.com which some are ipv6 enabled
# - 2 sources from 2.ubuntu.pool.ntp.org which is ipv6 enabled as well
# - 1 source from [01].ubuntu.pool.ntp.org each (ipv4 only atm)
# This means by default, up to 6 dual-stack and up to 2 additional IPv4-only
# sources will be used.
# At the same time it retains some protection against one of the entries being
# down (compare to just using one of the lines). See (LP: #1754358) for the
# discussion.
#
# About using servers from the NTP Pool Project in general see (LP: #104525).
# Approved by Ubuntu Technical Board on 2011-02-08.
# See http://www.pool.ntp.org/join.html for more information.
pool ntp.ubuntu.com        iburst maxsources 4
pool 0.ubuntu.pool.ntp.org iburst maxsources 1
pool 1.ubuntu.pool.ntp.org iburst maxsources 1
pool 2.ubuntu.pool.ntp.org iburst maxsources 2

# This directive specify the location of the file containing ID/key pairs for
# NTP authentication.
keyfile /etc/chrony/chrony.keys

# This directive specify the file into which chronyd will store the rate
# information.
driftfile /var/lib/chrony/chrony.drift

# Uncomment the following line to turn logging on.
#log tracking measurements statistics

# Log files location.
logdir /var/log/chrony

# Stop bad estimates upsetting machine clock.
maxupdateskew 100.0

# This directive enables kernel synchronisation (every 11 minutes) of the
# real-time clock. Note that it can’t be used along with the 'rtcfile' directive.
rtcsync

# Step the system clock instead of slewing it if the adjustment is larger than
# one second, but only in the first three clock updates.
makestep 1 3

# ============================================================================
# RAF CCTV - MELAYANI NTP UNTUK KAMERA  (ditambahkan 2026-08-26)
#
# Sebelumnya box ini memakai systemd-timesyncd, dan itu KLIEN saja: ia menyetel jam
# sendiri tapi tidak pernah bisa menjawab permintaan NTP. Terukur: tidak ada apa pun
# yang mendengarkan di UDP 123, jadi kamera yang diarahkan ke server ini tidak pernah
# mendapat jawaban - dan jam kamera Tiandy tertinggal di 1970-01-05.
#
# Subnet di bawah adalah tempat kamera berada (terverifikasi bisa di-ping dari box ini
# lewat gateway 172.17.11.1), ditambah LAN box itu sendiri.
# ============================================================================
allow 192.168.13.0/24
allow 192.168.16.0/24
allow 172.17.0.0/16

# Tetap melayani waktu meski hulu internet putus. Untuk CCTV, SATU waktu bersama yang
# sedikit meleset jauh lebih berguna daripada tiap kamera berjalan sendiri-sendiri -
# rekaman dari kamera berbeda harus bisa disandingkan pada peristiwa yang sama.
# Stratum 10 membuatnya jelas-jelas kalah dari sumber sungguhan begitu internet pulih.
local stratum 10
# ==== RAF CCTV - MELAYANI NTP MULAI ====
# Ditulis otomatis oleh deployment/camera-time/setup.sh - jangan diedit tangan;
# jalankan ulang skripnya supaya subnet kamera baru ikut terbawa.
allow 192.168.12.0/24
allow 192.168.13.0/24
allow 192.168.14.0/24
allow 192.168.16.0/24
# Tetap melayani waktu meski hulu internet putus. Untuk CCTV, SATU waktu bersama yang
# sedikit meleset jauh lebih berguna daripada tiap kamera berjalan sendiri-sendiri:
# rekaman dari kamera berbeda harus bisa disandingkan pada peristiwa yang sama.
# Stratum 10 membuatnya jelas kalah dari sumber sungguhan begitu internet pulih.
local stratum 10
# ==== RAF CCTV - MELAYANI NTP SELESAI ====
