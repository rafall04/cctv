<!--
Purpose: Local map for admin area management presentation components.
Caller: Agents and maintainers before editing admin area UI components.
Deps: ../../../pages/.module_map.md, AreaManagement.jsx, areaCoverage utilities, router links.
MainFuncs: Maps area cards, summaries, modal pieces, and focused verification paths.
SideEffects: None; documentation only.
-->

# components/admin/areas/.module_map.md

## Ownership

- `AreaCard.jsx`: Repeated admin area card for location metadata, status badges, grid-default controls, and area action buttons.
- `AreaFormModal.jsx`: Presentation-only create/edit area form modal.
- `AreaFormModal.test.jsx`: Focused form modal render/change/submit/cancel coverage.
- `BulkPolicyPreview.jsx`: Presentation-only impact preview panel for area bulk policy operations.
- `BulkPolicyPreview.test.jsx`: Focused render/callback coverage for the preview panel.

## Boundaries

- Keep page data loading, mutations, notifications, and modal state in `pages/AreaManagement.jsx` or hooks.
- Keep repeated visual sections here when they only render props and emit callbacks.
- Keep pure option lists or payload helpers in `utils/admin/*` when shared by page and components.

## Verification

- Focused page gate: `cd frontend && npm test -- src/pages/AreaManagement.test.jsx`.
- Lint after component edits: `cd frontend && npm run lint`.
