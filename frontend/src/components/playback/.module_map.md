<!--
Purpose: Playback component module map for public/admin recording playback presentation slices.
Caller: Agents and maintainers before editing playback presentation components.
Deps: ../../pages/Playback.jsx, ../../hooks/playback, ../../utils/playbackUrlState.js, ../../services/recordingService.js.
MainFuncs: Maps playback component ownership and focused verification.
SideEffects: None; documentation only.
-->

# frontend/src/components/playback/.module_map.md

## Components

- `PlaybackHeader.jsx`: playback route header — title, share trigger, and the camera picker. Wiring only.
- `PlaybackCameraPicker.jsx`: the camera picker itself — thumbnail trigger + dialog with area chips, search, and grouped rows. Replaced a native `<select>`, which rendered as an unreadable wrapped list on Android; matching rules live in `../../utils/playbackCameraPicker.js`. **Shared, despite the name and folder:** `pages/TelegramArchiveLibrary.jsx` (admin) uses it too, having had the identical `<select>` problem. Two optional props keep both callers honest — `metaFor` swaps the per-row sub-line (archive shows segment counts instead of locations) and `allOption` adds an "everything" row that picks `null` (playback watches exactly one camera and omits it). Both default to the playback behaviour, so that page is unaffected.
- `PlaybackOptions.jsx`: access notice + auto-play toggle. Renders BELOW the video so the selector stays directly above the player.
- `PlaybackVideo.jsx`: video element shell, media controls, buffering/seeking/error/empty states.
- `PlaybackTimeline.jsx`: the detailed bar for the slice currently listed, plus (when `dayScope` is passed) the coverage strip and range picker above it. Geometry is memoised on `segments` and the bands sit behind their own `memo`: `currentTime` ticks ~4x/second, and rebuilding ~1,065 bands on each tick is what made this page heavy. `buildTimelineGeometry` is exported for tests.
- `PlaybackCoverageStrip.jsx`: the WHOLE recorded span as one strip — runs green, holes red, the listed slice outlined — drawn from the backend's `coverage.runs`. Clicking it opens that local day. This is the honesty layer that makes narrowing the list safe; do not let it inherit the list's date filter. **Shared:** `pages/customer/MyRecordings.jsx` (owner_full) uses it too — that scope also reaches the archive, so it had the same "whole history in one list" problem.
- `PlaybackRangePicker.jsx`: rolling-24h / Hari ini / Kemarin, a date field, and day arrows. The default is ROLLING, not "hari ini", which is nearly empty at 00:15. Hands back `null` for the rolling preset, so callers rebuild it with `rollingRange()` and measure the window from now. Shared with `MyRecordings.jsx`.
- `PlaybackSegmentList.jsx`: selectable recording segment list. `memo` + one `memo`'d row each, and the two `Intl` formatters live at module scope — `toLocaleDateString` builds a fresh formatter per call, which cost ~3,200 constructions per render at admin scope.
- `PlaybackUsageGuide.jsx`: static usage guidance with public preview policy copy.
- `PlaybackAccessPanel.jsx`: trial claim + paid package purchase, and the issued access key (with copy-to-clipboard).
- `PlaybackTokenAccess.jsx`: entering/activating an existing access key.

## Boundaries

- Keep media lifecycle, URL state, segment loading, and playback viewer tracking in `../../pages/Playback.jsx`, `../../hooks/playback/*`, and `../../utils/playbackUrlState.js`.
- Keep these components presentational; callbacks and policy values should be passed as props.
- Public playback must not expose admin-only wording or scope. Admin playback should hide public preview limitations.
- Public playback uses the shared `../landing/LandingMobileDock.jsx` for mobile Home/Map/Grid/Favorit/Playback navigation; do not add a second playback-only quick action card.

## Verification

- Focused component tests: `npm test -- src/components/playback/PlaybackVideo.test.jsx src/components/playback/PlaybackUsageGuide.test.jsx src/components/playback/PlaybackHeader.test.jsx src/components/playback/PlaybackCameraPicker.test.jsx src/utils/playbackCameraPicker.test.js`.
- Playback integration tests: `npm test -- src/pages/Playback.test.jsx src/utils/playbackSegmentSelection.test.js src/utils/publicShareUrl.test.js`.
