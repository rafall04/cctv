# RAF NET CCTV Frontend Configuration
# ====================================

# Backend API URL
# Development: http://localhost:3000
# Production: https://api-cctv.raf.my.id or http://your-server-ip:3000
VITE_API_URL=http://localhost:3000

# API Key - Required for API authentication
# Generate this key from the backend admin panel:
#   POST /api/admin/api-keys with admin credentials
# Or generate directly in the database
# This key is sent with every API request for validation
# IMPORTANT: Keep this key secret and never commit to version control
VITE_API_KEY=your-api-key-here
